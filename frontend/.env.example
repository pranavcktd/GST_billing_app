# Where Next.js forwards /api requests (the FastAPI service URL in production)
API_PROXY_TARGET=https://your-backend.up.railway.app
# Optional: call the API directly from the browser instead (then set CORS_ORIGINS on the backend)
# NEXT_PUBLIC_API_URL=
